<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartFrigde" parent="Theme.Material3.DayNight.NoActionBar">
    </style>
    <style name="green_btn" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/green_background</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="cancel" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/sliver</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="Theme.SmartFrigde" parent="Base.Theme.SmartFrigde" />
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>